
## Plan 027 — Phase 0 & Phase 1

- Verified Plan 026/PR #20 merged into `main`; synced local `main` with `origin/main`; created `plan/027-analytics-security-hardening` from up-to-date main.
- Created `plans/027-analytics-security-hardening.md`; reconciled stale "Plan 026 active" references in `AGENTS.md` and `.agents/state.md` to reflect Plan 027 as active.
- Phase 1: fixed workspace analytics read contract (`server/integrations/clickhouse/analytics.ts`) to scope by workspace_id + exact requested-window source_window_id prefix, preventing double-counting of overlapping sync windows and making positioning/geofencing honor the requested date window. Wired `buildingId`/`geofenceId` query filters through `summary.get.ts` (previously silently dropped). Added zod runtime validation to `sync.post.ts` body.
- Validation: `npm run lint` clean, `npm run typecheck` clean.

## Plan 027 — Phase 2

- Confirmed workspace deletion never cleaned up ClickHouse-owned workspace analytics.
- Added `deleteWorkspaceAnalytics()` (bounded, parameter-bound `ALTER TABLE ... DELETE WHERE workspace_id = ...` against an allowlist of exactly the four `analytics_workspace_*` tables; legacy tables untouched).
- Reordered `[id].delete.ts`: verify ownership -> ClickHouse cleanup (abort with 503 on failure, Postgres row untouched) -> Postgres delete. No partial "deleted" state can be reported.
- Validation: lint + typecheck clean.

## Plan 027 — Phase 3

- Added `server/utils/rate-limit.ts`: in-memory per-process fixed-window limiter, KISS (no Redis, matches single-Nitro-process architecture).
- Applied to `/api/auth/register` (5/min/IP) and `/api/auth/login` (10/min/IP), checked before any expensive work.
- Reordered register.post.ts: existing-account check now runs before `hashPassword` (was hashing first regardless of outcome).
- login.post.ts already skipped hashing for unknown emails; only rate limit added.
- Validation: lint + typecheck clean.

## Plan 027 — Phase 4

- Confirmed: workspace Situm config save verified READ_WRITE/READ_ONLY permission levels but never compared organizationId between primary and Viewer credentials.
- Added an organization-match check in `[...workspacePath].ts` PUT handler, funneled into the existing sanitized 422 response; fails before persistence.
- Validation: lint + typecheck clean. Cross-org live credential test is externally credential-gated (not available here); fix reuses already-verified authSession fields.
