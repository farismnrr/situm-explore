
## Plan 027 — Phase 0 & Phase 1

- Verified Plan 026/PR #20 merged into `main`; synced local `main` with `origin/main`; created `plan/027-analytics-security-hardening` from up-to-date main.
- Created `plans/027-analytics-security-hardening.md`; reconciled stale "Plan 026 active" references in `AGENTS.md` and `.agents/state.md` to reflect Plan 027 as active.
- Phase 1: fixed workspace analytics read contract (`server/integrations/clickhouse/analytics.ts`) to scope by workspace_id + exact requested-window source_window_id prefix, preventing double-counting of overlapping sync windows and making positioning/geofencing honor the requested date window. Wired `buildingId`/`geofenceId` query filters through `summary.get.ts` (previously silently dropped). Added zod runtime validation to `sync.post.ts` body.
- Validation: `npm run lint` clean, `npm run typecheck` clean.

## Plan 027 — Phase 2

- Confirmed workspace deletion never cleaned up ClickHouse-owned workspace analytics.
- Added `deleteWorkspaceAnalytics()` (bounded, parameter-bound `ALTER TABLE ... DELETE WHERE workspace_id = ...` against an allowlist of exactly the four `analytics_workspace_*` tables; legacy tables untouched).
- Reordered `[id].delete.ts`: verify ownership -> ClickHouse cleanup (abort with 503 on failure, Postgres row untouched) -> Postgres delete. No partial "deleted" state can be reported.
- Validation: lint + typecheck clean.

## Plan 027 — Phase 3

- Added `server/utils/rate-limit.ts`: in-memory per-process fixed-window limiter, KISS (no Redis, matches single-Nitro-process architecture).
- Applied to `/api/auth/register` (5/min/IP) and `/api/auth/login` (10/min/IP), checked before any expensive work.
- Reordered register.post.ts: existing-account check now runs before `hashPassword` (was hashing first regardless of outcome).
- login.post.ts already skipped hashing for unknown emails; only rate limit added.
- Validation: lint + typecheck clean.

## Plan 027 — Phase 4

- Confirmed: workspace Situm config save verified READ_WRITE/READ_ONLY permission levels but never compared organizationId between primary and Viewer credentials.
- Added an organization-match check in `[...workspacePath].ts` PUT handler, funneled into the existing sanitized 422 response; fails before persistence.
- Validation: lint + typecheck clean. Cross-org live credential test is externally credential-gated (not available here); fix reuses already-verified authSession fields.

## Plan 027 — Phase 5

- Added server/utils/bounded-fetch.ts (AbortController, 10s default, 5s for telemetry), applied to reports.ts, groups-alarms.ts, telemetry-logs.ts. Errors sanitized, no credential leakage.
- @situm/sdk-js 0.25.0 has an undocumented `timeouts?: Record<string,number>` SDKConfiguration field (axios-based); left unset per no-evidence-no-implementation -- key semantics not documented in .d.ts or README. Recorded as unresolved, not implemented.
- Validation: lint + typecheck clean.

## Plan 027 — Phase 6

- Added server/middleware/security-headers.ts: X-Content-Type-Options, Referrer-Policy, X-Frame-Options DENY, Permissions-Policy denying unused camera/mic/geolocation/payment/usb.
- Live-verified via local dev server curl -sI: all four headers present, app root still 200.
- CSP intentionally not shipped -- no proven allowlist of the hosted Situm Viewer's actual origins exists; documented as an open limitation rather than guessed.
- Validation: lint + typecheck clean.

## Plan 027 — Phase 7

- Decision: no new test framework. Used native `node --test` (Node 24) + already-present `tsx`; added `npm test` script.
- Added test/pure-logic.test.ts (8 tests): isValidDateRange, buildAnalyticsSyncKey, rateLimit, boundedFetch/UpstreamTimeoutError (live-proves abort within bound against a hung local HTTP server). All pass.
- Added test/regression/analytics-clickhouse.regression.ts (manual, needs reachable ClickHouse): imports and exercises the real production queryWorkspaceAnalytics/deleteWorkspaceAnalytics/ensureClickHouseSchema against the local shared-clickhouse instance. 10/10 checks passed: no double-counting of overlapping windows, date filtering excludes unrelated windows, workspace isolation, building/geofence filters both directions, deletion leaves zero workspace-owned rows while control workspace is untouched.
- Auth ordering (Phase 3) and org-mismatch (Phase 4) covered by direct code evidence rather than scripted tests (timing-signal/credential-gated).
- Validation: lint + typecheck clean, npm test all pass.

## Plan 027 — Phase 8 (closeout)

- Full validation: git diff --check, npm run lint, npm run typecheck, npm run build, npm test -- all clean/passing.
- Production-preview runtime smoke against the real .output/server/index.mjs bundle on an isolated port (existing staging container deploy-situm-explore-1 left untouched): root 200 with all Phase 6 security headers live; unauthenticated GET /api/workspaces still 401; POST /api/auth/login rate limit trips 429 after 10 requests/min (generic 401 unchanged below limit); POST /api/auth/register rate limit trips 429 after 5 requests/min.
- Cleaned up synthetic test accounts (rl-test-1..5@example.com) from situm_explore.users after the smoke.
- Plan 027 marked complete. No PR or merge performed -- branch pushed and synchronized with origin, awaiting user-directed review.

## Plan 027 — Review remediation (post-Phase-8)

- Reconfirmed all 6 review findings against current code before changing anything; all 6 valid, none stale.
- Finding 1 (spoofable rate-limit identity): confirmed `deploy/staging.compose.yml` publishes Nitro directly on the host port, no reverse proxy -- `getRequestIP` switched from `xForwardedFor: true` to `false` in `server/utils/rate-limit.ts`. Live-verified: 12 login attempts each with a distinct spoofed X-Forwarded-For still hit 429 after 10.
- Finding 2 (unbounded limiter memory): added self-pruning of expired buckets on every `rateLimit()` call, no timers. New test proves a previously-exhausted key becomes usable again after its window elapses (bucket was collected).
- Finding 3 (weak reset test): `rateLimit resets after the window elapses` now actually awaits past the window and re-asserts allowed, deterministic (30ms window).
- Finding 4 (trivial isolation assertion): replaced the `!Object.keys(...).includes('workspaceB')` no-op with real cross-workspace queries against the actual `queryWorkspaceAnalytics` production function.
- Finding 5 (missing sync_runs coverage): `deleteWorkspaceAnalytics` already covered all 4 tables in production code; regression script only checked 3 -- added synthetic sync-run rows for workspace A and control workspace B and asserted correct deletion/survival.
- Finding 6 (undeclared dotenv): removed the `dotenv` import from the regression script (confirmed only a transitive/hoisted dep, not in package.json); manual run command now uses `--env-file=.env`.
- Validation: `git diff --check`, `npm run lint`, `npm run typecheck`, `npm run build`, `npm test` (9/9) all pass; manual ClickHouse regression 17/17 pass.
- Updated `plans/027-analytics-security-hardening.md` (new "Review remediation" section), `.agents/state.md`, and `.agents/memory/decisions.md` (new trusted-proxy stance note under Auth abuse protection) before committing.
- Committed and pushed to `origin/plan/027-analytics-security-hardening`. No PR/merge performed.

## Native companion roadmap planning — Plans 028–032

- User approved the native companion direction after Plan 027 integration: React Native + Expo development builds + `@situm/react-native`, while preserving Nitro as the single backend and the existing PostgreSQL user/workspace model.
- Product policy frozen for planning: desktop/tablet web Map remains available where Viewer usability is accepted; phone web Map hands off to native; all web Realtime entry points hand off to native after native Realtime exists.
- Created `roadmap/028-032-native-mobile` from clean integrated `origin/main` (PR #21 merge baseline).
- Added roadmap plus five sequential executable plans: Plan 028 capability/auth/distribution spike; Plan 029 native foundation/workspace session; Plan 030 native Map/positioning/navigation; Plan 031 native Realtime; Plan 032 web/native handoff/distribution/full regression.
- Current official Situm evidence confirms an Expo development-build path and current React Native MapView/positioning/navigation support; native Android/iOS SDKs expose newer JWT-token auth, but the public React Native token surface remains a Plan 028 hard evidence gate rather than an assumption.
- Reconciled `AGENTS.md`, `ARCHITECTURE.md`, `plans/README.md`, `.agents/state.md`, `.agents/memory/decisions.md`, and `design/data-source-matrix.md` so Plan 027 is integrated truth and the new native roadmap is planned rather than falsely implemented.
- No native product implementation was performed in this planning pass.

## Remediation / Audit

- Corrected documentation to reflect parent review findings.
- Clarified that `.agents/memory/roadmap-021-025.md`, `plans/021-025-prerequisites.md`, and `design/ROADMAP-021-025-OVERRIDES.md` are historical context only.
- Made explicit that the roadmap planning branch is the planning authority until integrated, after which Plan 028 is executable.
- Updated root `README.md` to state Plans 017-027 are complete/integrated (Plan 027 via PR #21) and the architecture consists of current Nuxt/Nitro plus a planned React Native second client with no second backend.
- Updated `design/data-source-matrix.md` to clarify that workspace-scoped behavior is integrated and Plans 028-032 represent additive native-client work, not unfinished backend migration.
- Broader parent audit also reconciled stale completed-roadmap wording in durable decisions: integrated Viewer direct Read-only API-key auth, workspace-scoped Situm/analytics, observability, and Plan 026/027 runtime decisions are now described as current runtime boundaries rather than future plan work.
- Re-ran semantic stale-authority searches and validation. Plan 028 has not started.


## Plan 028 — Phase 0

- Roadmap PR #22 merged successfully at `e9091107f6676e15a0a5887629bb62e84aede0aa`; roadmap branch was deleted local/remote and `main` synchronized before Plan 028 branch creation.
- Created `plan/028-native-capability-auth-spike` from exact integrated `origin/main`; working tree started clean.
- Worker performed Phase-0 pre-flight/source discovery. Parent rejected its generic JWT wording and independently verified current official Situm sources.
- Current official evidence: RN quickstart updated 2026-04-17 requires Expo development builds/prebuild (not Expo Go); RN changelog updated 2026-08-13 has 3.19.2 latest and 3.19.0 JWT wrapper support; positioning guide updated 2026-08-05; API-key guidance updated 2026-03-16; Android/iOS native changelogs separately expose token auth from 3.37.0/3.40.0.
- No Phase-0 blocker. Exact dependency matrix remains Phase 1; exact React Native token format/lifetime/refresh contract remains Phase 3. No native product implementation performed.
