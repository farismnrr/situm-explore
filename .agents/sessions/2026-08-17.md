
## Plan 027 — Phase 0 & Phase 1

- Verified Plan 026/PR #20 merged into `main`; synced local `main` with `origin/main`; created `plan/027-analytics-security-hardening` from up-to-date main.
- Created `plans/027-analytics-security-hardening.md`; reconciled stale "Plan 026 active" references in `AGENTS.md` and `.agents/state.md` to reflect Plan 027 as active.
- Phase 1: fixed workspace analytics read contract, wired building/geofence filters, and added zod runtime validation to sync input.
- Validation: `npm run lint` clean, `npm run typecheck` clean.

## Plan 027 — Phase 2

- Added bounded ClickHouse cleanup for workspace deletion and abort-on-failure behavior before Postgres deletion.
- Validation: lint + typecheck clean.

## Plan 027 — Phase 3

- Added in-memory fixed-window rate limiting to registration/login and corrected registration hashing order.
- Validation: lint + typecheck clean.

## Plan 027 — Phase 4

- Added Situm primary/Viewer organization matching before workspace credential persistence.
- Validation: lint + typecheck clean.

## Plan 027 — Phase 5

- Added bounded fetch handling to app-owned upstream calls; left undocumented SDK timeout configuration unset per the evidence rule.
- Validation: lint + typecheck clean.

## Plan 027 — Phase 6

- Added security headers and intentionally left CSP deferred pending verified hosted Viewer origins.
- Live-verified headers with local curl; lint + typecheck clean.

## Plan 027 — Phase 7

- Added pure-logic and manual ClickHouse regression coverage without adding a test framework.
- Full local checks and the manual analytics regression passed.

## Plan 027 — Phase 8 (closeout)

- Full validation and production-preview smoke passed; synthetic smoke accounts were removed.
- Plan 027 marked complete with no PR/merge performed.

## Plan 027 — Review remediation

- Repaired rate-limit proxy trust and pruning, strengthened the window reset and workspace isolation assertions, covered sync-run cleanup, and removed an undeclared dotenv import.
- Validation: diff check, lint, typecheck, build, tests, and ClickHouse regression passed; branch pushed with no PR/merge.

## Native companion roadmap planning — Plans 028–032

- User approved the native companion direction: React Native + Expo development builds + `@situm/react-native`, preserving Nitro and the existing PostgreSQL identity/workspace model.
- Created the roadmap and five sequential plans, and reconciled repository authority documents. No native product implementation was performed in the planning pass.

## Plan 028 — Phase 0

- Roadmap PR #22 merged; created `plan/028-native-capability-auth-spike` from exact integrated `origin/main`.
- Verified current official Situm evidence for Expo development builds, React Native 3.19.2, positioning, API-key guidance, and native token support. Exact version and token contract remained later gates.

## Plan 028 — Phases 1–6

- Proved the frozen Expo/RN/Situm capability matrix, dedicated encrypted Positioning credential contract, sealed h3 application session over `x-nuxt-session`, SecureStore boundary, identifiers/deep-link contract, and local build/distribution workflow.
- Explicitly retained gates for published TypeScript resolution, server revocation/version checks, Android/iOS runtime, public HTTPS association, and signing/store delivery. Plan 028 closed with Plan 029 GO readiness and no native implementation.

## Plan 029 execution — Phase 0

- User requested full execution of Plan 029 on `plan/029-native-app-foundation`, stopping before PR/merge.
- Read repository authority and execution brief; confirmed clean branch at the integrated Plan 028 base.
- Revalidated published frozen versions: Expo 57.0.13, `@situm/react-native` 3.19.2, `expo-secure-store` 15.0.8, and JDK 21.0.10.
- Updated Plan 029 status/checklist and current state. Next: create the standalone mobile package and inspect the published wrapper typings there.

## Plan 029 — Phase 1

- Created standalone `mobile/` Expo 57.0.13 package with frozen React/RN/Situm/WebView/SecureStore versions, strict TypeScript, app identifiers, custom schemes, light responsive shell seed, and ignored native/signing output.
- Revalidated `@situm/react-native` 3.19.2 inside the real package: declared `lib/` is absent while the documented `src/` React Native entry exists. Used a narrow TypeScript path to the published source; no vendoring/forking.
- `expo prebuild --clean --no-install`, mobile lint, and typecheck pass. Gradle configuration confirms Android 24/36/36 and Kotlin 2.1.20; assembleDebug is blocked only by the host's missing Android SDK. iOS remains macOS/device-gated.

## Plan 029 — Phase 2

- Added the typed mobile API boundary with environment-only base URL, `x-request-id` correlation, optional `x-nuxt-session`, bounded AbortController timeout, and safe normalized product errors.
- Mobile lint and typecheck pass. No raw backend proxy, credential, database, or upstream access was introduced.

## Plan 029 — Phase 3

- Added mobile login returning the same sealed h3 session over `x-nuxt-session`, explicit seven-day age, user session-version validation middleware, and server-invalidating logout.
- Added encrypted-at-rest dedicated Positioning credential persistence/validation and owner-scoped issuance; primary Read & Write and browser Viewer credentials remain excluded.
- Root lint, typecheck, build, migration generation, and mobile lint/typecheck pass. Runtime/device acceptance remains gated on configured database/credentials and Android/iOS targets.
