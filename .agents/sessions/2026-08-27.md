# Session — 2026-08-27

## 12:31 — Web loading-state hygiene review

- User wanted: full review and fix for transient dummy/empty copy appearing before skeletons, explicitly including Alarms (`Select a building`) and Map.
- Actions/decisions: traced the issue to Nuxt `useFetch(..., { immediate: false })` starting in `idle`; added shared workspace-aware unresolved-state helpers; applied skeleton-first precedence across workspace web surfaces; gated `SitumViewer` until real cartography/building resolution; changed Viewer initialization to skeleton-only loading; added regression tests.
- Durable updates: `DESIGN.md` now states that unresolved workspace context plus `idle`/`pending` requests are loading and that data-derived empty/count/configuration/unavailable states wait for resolution. Plan 037 records implementation scope and validation.
- Validation: `git diff --check`; `npm test` 74/74; `npm run lint`; `npm run typecheck`; `npm run build` all passed.
- Open loop: branch is not deployed, opened as a PR, or merged; runtime visual acceptance can be done when deployment is explicitly requested.

## 13:00 — Plan 037 closeout and stacked Plan 038 two-key credentials

- User wanted: finish Plan 037, then immediately simplify Situm credentials to exactly `Only Read` + `Read & Write`; APK must keep requesting its key from the backend and must not receive Read & Write.
- Plan 037: validated 74/74 tests + lint + typecheck + build + `git diff --check`, committed `cb00201c` (`fix: stabilize workspace loading states`), and pushed `plan/037-loading-state-hygiene`. No PR/merge/deploy was performed.
- Authorization: user explicitly requested continuing Plan 038 after Plan 037, so Plan 038 was stacked from `cb00201c` rather than waiting for main integration.
- Evidence: current Situm docs reconfirm Only Read includes positioning/read access, mobile SDK accepts generic API-key initialization, and Read & Write is the broad internal/server authority. Installed JS SDK permission enum matches the expected permission levels.
- Implementation: removed dedicated Positioning configuration/storage/runtime use; mobile `/mobile-positioning` now owner-scopes and issues the stored Only Read key; browser Viewer and existing server read/report helpers use Only Read; a distinct server-only Read & Write helper remains for mutation/admin paths; workspace UI exposes only two independently replaceable credential fields with actionable permission/org errors.
- Migration: generated `drizzle/0009_unusual_wrecking_crew.sql`, which makes the Read & Write column nullable and drops the legacy encrypted Positioning column. It was not applied to a real database because dropping that column is destructive and production migration was not authorized.
- Validation: root `git diff --check`, 75/75 tests, lint, typecheck, build; mobile lint and typecheck all passed.
- Open loop: runtime acceptance remains pending—deploy/migrate under explicit approval, then verify browser Viewer plus physical Android positioning with a real Only Read key and confirm Read & Write is absent from client payloads.
