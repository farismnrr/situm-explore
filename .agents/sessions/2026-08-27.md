# Session — 2026-08-27

## 12:31 — Web loading-state hygiene review

- User wanted: full review and fix for transient dummy/empty copy appearing before skeletons, explicitly including Alarms (`Select a building`) and Map.
- Actions/decisions: traced the issue to Nuxt `useFetch(..., { immediate: false })` starting in `idle`; added shared workspace-aware unresolved-state helpers; applied skeleton-first precedence across workspace web surfaces; gated `SitumViewer` until real cartography/building resolution; changed Viewer initialization to skeleton-only loading; added regression tests.
- Durable updates: `DESIGN.md` now states that unresolved workspace context plus `idle`/`pending` requests are loading and that data-derived empty/count/configuration/unavailable states wait for resolution. Plan 037 records implementation scope and validation.
- Validation: `git diff --check`; `npm test` 74/74; `npm run lint`; `npm run typecheck`; `npm run build` all passed.
- Open loop: branch is not deployed, opened as a PR, or merged; runtime visual acceptance can be done when deployment is explicitly requested.

## 13:00 — Plan 037 closeout and stacked Plan 038 two-key credentials

- User wanted: finish Plan 037, then immediately simplify Situm credentials to exactly `Only Read` + `Read & Write`; APK must keep requesting its key from the backend and must not receive Read & Write.
- Plan 037: validated 74/74 tests + lint + typecheck + build + `git diff --check`, committed `cb00201c` (`fix: stabilize workspace loading states`), and pushed `plan/037-loading-state-hygiene`. No PR/merge/deploy was performed.
- Authorization: user explicitly requested continuing Plan 038 after Plan 037, so Plan 038 was stacked from `cb00201c` rather than waiting for main integration.
- Evidence: current Situm docs reconfirm Only Read includes positioning/read access, mobile SDK accepts generic API-key initialization, and Read & Write is the broad internal/server authority. Installed JS SDK permission enum matches the expected permission levels.
- Implementation: removed dedicated Positioning configuration/storage/runtime use; mobile `/mobile-positioning` now owner-scopes and issues the stored Only Read key; browser Viewer and existing server read/report helpers use Only Read; a distinct server-only Read & Write helper remains for mutation/admin paths; workspace UI exposes only two independently replaceable credential fields with actionable permission/org errors.
- Migration: generated `drizzle/0009_unusual_wrecking_crew.sql`, which makes the Read & Write column nullable and drops the legacy encrypted Positioning column. It was not applied to a real database because dropping that column is destructive and production migration was not authorized.
- Validation: root `git diff --check`, 75/75 tests, lint, typecheck, build; mobile lint and typecheck all passed.
- Open loop: runtime acceptance remains pending—deploy/migrate under explicit approval, then verify browser Viewer plus physical Android positioning with a real Only Read key and confirm Read & Write is absent from client payloads.

## 13:41 — Plan 038 local production-style deployment

- User authorization: explicit `gas deploy` authorized the previously gated schema migration and local production-style Compose deployment. No raw credential values were inspected or printed.
- Database preflight: one workspace Situm configuration row existed; the legacy Positioning credential column existed and contained one configured value; Read & Write was still NOT NULL; Drizzle had 38 applied migration rows.
- Safety: created a PostgreSQL custom-format backup of schema `situm_explore` at `/tmp/situm-explore-plan038-pre-migration-20260827-134152.dump` with mode 0600 before applying the destructive migration.
- Migration: `npm run db:migrate` applied migration `0009_unusual_wrecking_crew`; afterward the configuration row remained, Read & Write is nullable, the legacy Positioning column is absent, and the migration journal has 39 rows. Metadata-only checks confirmed the existing row still has both Only Read and Read & Write configured without reading encrypted values.
- Image/deploy: GHCR publish of `sha-5ed21ddef47b` failed with registry authorization HTTP 403. To avoid blocking the laptop runtime, built commit `5ed21ddef47b3acb106141d15b1dd43e3bb0d0be` locally as `ghcr.io/farismnrr/situm-explore:staging` and force-recreated `deploy-situm-explore-1` without pulling from the registry.
- Runtime proof: container image ID matches the freshly built local image; container is `running/healthy`; `/api/health/liveness` returned 200; `/` returned 200; unauthenticated `/api/workspaces` returned 401; `make staging-smoke` passed; recent container logs contained only the normal Nitro listen line.
- Build warning: the Dockerfile still pins Node 22.14.0 while current Nuxt 4.5.2 declares Node >=22.19.0. The image builds and runtime smoke passes, but the engine mismatch should be corrected in a follow-up rather than silently ignored.
- Remaining acceptance: GHCR remote staging tag is not published; authenticated browser Viewer behavior and physical Android positioning with backend-issued Only Read are not yet runtime-proven in this deployment.

## 14:00 — Plan 038 runtime hardening and acceptance preflight

- Updated both Docker build stages from Node `22.14.0-bookworm-slim` to `22.22.0-bookworm-slim`; clean production-image build completed and the Nuxt Node engine mismatch warning disappeared. Informational plugin-timing and system-user-UID warnings remain only.
- Verified current local runtime is healthy but still the previous `5ed21dd` image; DB metadata remains secret-free: the workspace Situm table has exactly two encrypted credential columns, both configured, and the migration journal contains `0009`.
- GHCR read metadata is reachable and local Docker/`gh` auth configuration exists without printing credentials; final push will be attempted after the final commit. The existing authenticated tunnel tab currently shows the old three-key UI and will be rechecked after local staging is recreated from the final image.
- `adb devices` found no physical Android target. Mobile lint/typecheck and release-artifact checks remain executable, but sensor-backed Only Read positioning cannot be claimed until a device is connected.
- Durable changes: Plan 038 now records the Node runtime hardening and `.agents/state.md` records the current acceptance gates without secrets.
- Open loop: finish final image/push/recreate, browser and API acceptance, mobile/static security checks, evidence/docs closeout, then PR/merge/branch cleanup only if required gates pass.

## 14:07 — Viewer lifecycle regression fixed from authenticated acceptance

- Reproduced the deployed Map Viewer blocker: workspace cartography returned HTTP 200 and the Viewer component mounted, but no `/viewer-auth` request occurred and the skeleton never resolved.
- Root cause: `SitumViewer`'s immediate prop watcher ran before the `root` DOM ref was mounted; its initialization guard returned and there was no mounted retry. Similar DOM-owning components use `onMounted`.
- TDD evidence: added the minimal lifecycle regression assertion, observed the targeted web test fail 5/6, added `onMounted(() => { void initialize() })`, then observed the targeted test and full root suite pass (75/75).
- No credential values were inspected; the authenticated browser showed only request paths and safe UI state. Final image push/recreate and browser rerun remain pending for this new commit.

## 14:30 — Plan 038 runtime acceptance closeout

- Final image built from `1a18aa8` with Node `v22.22.0`, pushed successfully to GHCR under the immutable commit tag and `staging`, and verified pullable at digest `sha256:b0b0f551e6b5f67f440c5b7f753a0fbe8412b020744b062bb22a1f688411a79d`. `deploy-situm-explore-1` was force-recreated from that image and is `running/healthy`, with liveness/root/authorization-boundary and `make staging-smoke` passing.
- Authenticated browser acceptance after final recreate verified the configured workspace exposes exactly `Only Read API key` and `Read & Write API key`, independent configured statuses, no Positioning field/copy, and a rendered Map Viewer. Cartography showed one building and two floors; route probes reached expected read-only surfaces without error alerts.
- Security checks found no active legacy Positioning contract in source, no encrypted credential storage names or legacy identifiers in public web output, and no legacy identifiers in the release APK strings. Recent runtime logs contained no raw key/error output.
- Android release build passed for arm64 (`v1.0.2`, versionCode `3`); `adb devices -l` remains empty, so no install, runtime permission, sensor fix, or device realtime acceptance is claimed.
- Updated Plan 038, Plans README, state, session, and dedicated evidence with this exact gate decision. PR/merge/branch deletion are intentionally not performed while the physical Android gate is unavailable; no secret values were accessed or recorded.

## 14:42 — Final image revision aligned with closeout commit

- Pushed the documentation/evidence closeout commit `bddd72889135429df9f6fac5880e15e1795badac`, rebuilt and republished the production image, and force-recreated the staging container so the runtime revision matches the closeout source.
- Final GHCR `staging` and immutable commit image digest is `sha256:75739ca6d557fc9c5098879cc668284581ee77b61a1d9889df5376ad2b0b6f58`; the container reports revision `bddd72889135429df9f6fac5880e15e1795badac`, Node `v22.22.0`, and `running/healthy`.
- This correction only aligns the written evidence with the final deployment metadata; no application secret was accessed and no product data was changed.

## 14:47 — Final backup-retention consistency check

- Final preflight verification confirmed the runtime database still has one workspace configuration row, both encrypted credential slots populated, and zero legacy Positioning columns.
- The previously created mode-0600 pre-migration dump path `/tmp/situm-explore-plan038-pre-migration-20260827-134152.dump` is no longer present. This is recorded as a temporary-directory retention issue; no post-migration dump is substituted or mislabeled, and no database rollback/rewrite was attempted.
- Branch `plan/038-two-key-situm-credentials` remains clean and synchronized with origin at the documentation closeout commit. Physical Android acceptance remains the primary external gate; backup retention is an additional closeout caveat.
