# Session — 2026-08-13 — Plans 017–020 roadmap handoff

## User direction

- User closed the manual UI refinement session and asked to resume feature development.
- Latest `main` already includes UI refinement PRs #10 and #11; that is the code/UI baseline for the next roadmap.
- User requested Plans 017–020 to be prepared together and asked for an unattended full-run prompt.
- User explicitly selected ClickHouse for Plan 017 and stated a ClickHouse installation/instance already exists locally on the laptop; agents should reuse it rather than provision another one.
- User explicitly authorized stacked sequential execution for Plans 017→018→019→020 with no PR/merge during execution.
- Every implementation phase must be delegated specifically to the configured `worker` subagent. Parent agent owns orchestration/review/persistence/commit/push/phase transitions. No worker-profile substitution is allowed.

## Current roadmap

- Plan 017 — `plans/017-situm-analytics-clickhouse.md`
  - Situm Reports -> authenticated Nitro ingestion -> existing local ClickHouse -> app analytics API/UI.
  - Core: visitors, positioning time, geofence stay time, ClickHouse-backed CSV export.
  - Explicit product sync; no background queue/cron.
- Plan 018 — `plans/018-situm-groups-alarms-read.md`
  - Exact-contract/runtime verification then Groups + Alarms read-only UI/API.
- Plan 019 — `plans/019-situm-realtime-viewer-trajectory.md`
  - Viewer-owned realtime overlay via verified SDK methods; trajectory is conditional on exact runtime evidence.
- Plan 020 — `plans/020-situm-static-directions.md`
  - Viewer static directions between known points/POIs; explicitly excludes live navigation/current-location semantics.

## Branch chain

```text
roadmap/017-020-next-features
-> plan/017-situm-analytics-clickhouse
-> plan/018-situm-groups-alarms-read
-> plan/019-situm-realtime-viewer-trajectory
-> plan/020-situm-static-directions
```

Each next branch must start from the previous plan's final validated/pushed HEAD.

## Evidence captured while planning

- Current official Situm REST documentation exposes Reports families with JSON/CSV output, including visitors, positioning time, geofence reports, heatmap, raw data, user positions, and Map Viewer usage.
- Current official Viewer JS documentation exposes `loadRealtimePositions` / `cleanRealtimePositions`, `loadTrajectory` / `cleanTrajectory`, and static directions methods including `startDirections`, `startDirectionsByExternalId`, `cancelDirections`, and `directionsSetOptions`.
- Installed repository dependency at planning time: `@situm/sdk-js ^0.25.0`; each implementation plan must still inspect the actual installed version/types/runtime before coding.
- No ClickHouse integration currently exists in the repository; Plan 017 must discover the real local ClickHouse connection safely and add only the smallest server-side integration required.

## Durable updates made

Updated on `roadmap/017-020-next-features`:

- four executable plan files 017–020;
- `.agents/state.md`;
- `.agents/memory/decisions.md`;
- `.agents/README.md`;
- `AGENTS.md`;
- `plans/README.md`;
- `ARCHITECTURE.md`;
- `design/data-source-matrix.md`;
- this session handoff.

The abandoned `chore/ui-refine-login-map-feedback` branch is superseded by the manually refined UI already integrated into `main`; do not use it as a base.

## Next action

Start on the final HEAD of `roadmap/017-020-next-features`, create `plan/017-situm-analytics-clickhouse`, and execute the full stacked roadmap using the user's worker-only orchestration prompt. Do not create a PR or merge. Stop only for a real core blocker or inability to spawn the configured `worker`; otherwise continue automatically through Plan 020.
