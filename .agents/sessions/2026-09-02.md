# 2026-09-02

## Plan 041 — navigation-first native map UI

- User requested a direct React Native Explore redesign inspired by the supplied indoor-navigation reference, with plan-through-physical-E2E execution on the connected Android device.
- Correct task workspace was explicitly set to `/home/farismnrr/Projects/situm-explore` and verified against `https://github.com/farismnrr/situm-explore.git` before mutation.
- Created `plan/041-navigation-first-map-ui` from current `origin/main` and persisted the implementation plan.
- PHASE-01 verified installed `@situm/react-native@3.19.2` source: navigation progress provides real distance/time/indication data and MapView retains POI/floor/navigation/follow controls. Added narrow local types plus deterministic distance/ETA/indication helpers and focused tests.
- Mobile lint revealed an existing undeclared direct import of `@typescript-eslint/parser`; added it as a mobile dev dependency. Mobile lint/typecheck and the new focused tests pass.
- PHASE-02–03 implementation now redesigns Explore around real current-building POI search, destination selection, floor controls, location/fullscreen controls, and a real Situm navigation HUD using verified distance/time/indication callbacks. Focused native regressions pass 36/36 plus mobile lint/typecheck.
- First branch push attempt failed because the MCP sandbox has no HTTPS GitHub credential; local commits are retained and execution continues locally without bypassing auth.
- Next: PHASE-04 Android build/install, then ADB trial/error acceptance and bounded UI fixes.

## Plan 041 — final branded splash/release acceptance

- Reworked the launch handoff around the supported `expo-splash-screen` config plugin and an app-owned React Native `Animated` continuation using the existing `assets/splash-icon.png`; no Lottie/video dependency was introduced.
- The Android 11 POS exposed a debug-shell handoff trap while trialing layout-driven release. The accepted implementation keeps `preventAutoHideAsync()` at module scope, releases the native splash from the mounted launch overlay on the next animation frame with synchronous `SplashScreen.hide()`, then runs logo pulse/halo/wordmark animation and waits for both animation + bootstrap readiness before fading the overlay.
- Added regression coverage in `test/mobile-splash.test.ts` and hardened Android build scripts so routine device/release iteration preserves arm64-only builds, non-clean Expo Prebuild, a persistent Gradle cache, and explicit `adb install -r -d` for the lower-version debug shell. `AGENTS.md` now records the same build-safety rule to prevent agents from accidentally regenerating/building four ABIs.
- Final automated gates: mobile typecheck PASS, mobile lint PASS, splash tests 3/3 PASS, full root tests **92/92 PASS**, and `git diff --check` PASS.
- Final repository release wrapper PASS: `npm --prefix mobile run build:android:release` completed in **1m 53s** with `411/422` tasks up-to-date. Artifact `mobile/dist/situm-explore-v1.0.2-android-arm64.apk` has SHA-256 `60ed6a8b5a589364dc61497bf2c0c7c05926fa417586885aba9467d853d9b165`; checksum verification passes and APK native libraries contain only `arm64-v8a`.
- Reinstalled that final `dist` artifact on ADB target `100.113.52.76:37371` with data preserved and recorded a new 5.05s cold launch. Android reported `LaunchState: COLD`, `TotalTime: 1519 ms`, the main activity resumed normally, and the bounded logcat slice contained no `FATAL EXCEPTION` or `E/ReactNativeJS` entry.
- Logo-only frame analysis intentionally excluded the wordmark. The release recording measures the centered white logo at **88×88 px** before the app-owned pulse, **95×94 px** at pulse peak, then approximately **90×89 px** after settling. This is direct pixel evidence that the logo itself moves; acceptance does not rely only on the wordmark fade.
- Temporary release recordings remain ignored under `mobile/.expo/` and are not committed. Movement-dependent arrival/off-route reproduction still requires physically moving the POS through the venue and remains explicitly unclaimed rather than synthesized.

## Repository testing policy change

- User explicitly required removal of every repo-authored unit test and prohibited persistent unit tests going forward.
- Persistent regression/test harnesses and their package scripts were removed as part of the same policy change.
- Future E2E, white-box, or black-box checks may be created only temporarily and must be deleted before staging/commit/closeout; durable gates are lint/typecheck/build plus runtime/browser/device evidence appropriate to the task.
- Removed the persistent `staging-smoke` Make target as a black-box harness; operational shell guards and container healthchecks remain runtime configuration, not test suites.
- Post-removal validation: `npx nuxt prepare`, root lint/typecheck/build, mobile lint/typecheck, `git diff --check`, package JSON parsing, and a filesystem/package-script audit all passed with zero repo-authored persistent test files/directories or test commands. No unit test was run.

## Repository history and branch cleanup

- User explicitly authorized PR/merge to `main` plus cleanup of stale local/remote branches after reviewing Git history and repository memories.
- Audit classification: local `feature/mobile-update-password-keyboard-ux` and `roadmap/033-034-native-ui-reconciliation` are fully contained in `main`; `plan/039-workspace-situm-isolation` and `plan/040-mobile-explore-fullscreen` are patch-equivalent to their already merged PR #37/#38 results; none requires another merge or cherry-pick.
- `release/android-v1.0.2` contained two unique documentation/evidence commits not present on `main`. Rather than cherry-pick the stale release-branch state wholesale, only `.agents/evidence/android-release-1.0.2-2026-08-28.md` and `.agents/sessions/2026-08-28.md` were preserved on Plan 041 before integration.
- Plan 041 was pushed as PR #39 and squash-merged into `main` at `99318a608af24f036e1744d9e7e929b859fbe15e`; GitGuardian passed and the change request reported CLEAN/MERGEABLE before merge.
- After PR #39, no unique product or release history remains stranded on the stale branches identified above; they are eligible for deletion.
