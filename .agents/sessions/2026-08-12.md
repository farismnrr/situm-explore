# Sessions — 2026-08-12

## Initial scaffold — Self-improvement agent foundation

- User wanted a Codex-oriented self-improvement agent with a root `AGENTS.md` that routes persistent context into `.agents/`.
- The repository was empty, so the agent foundation was initialized from scratch.
- Added a Markdown-first context model for identity, state, chat lifecycle, persistence, memory, knowledge, reflections, and session history.
- Established a mandatory persistence pass for every conversation: every chat gets a session trace, while durable stores update only when meaningful information changes.
- Persisted the current goal, workspace preferences, and architecture decisions.
- Product/application implementation remained intentionally deferred at this stage.
- Open loop: validate the structure through normal Codex conversations and refine it based on real usage.

## Web architecture planning

- User moved the project into its first web phase and asked for a concrete implementation checklist under `plans/`.
- Chosen direction: single full-stack Nuxt app, Nuxt UI, Situm web-first integration, existing PostgreSQL with a dedicated project schema, Drizzle ORM, and a maintained Nuxt-oriented auth plugin/module.
- Native/mobile remains out of scope.
- User explicitly wants low complexity and no premature architecture.
- Existing PostgreSQL must be explored by Codex before migrations; unrelated schemas/tables must not be modified.
- Situm credentials will be supplied locally through environment configuration and must not be committed.
- Added `plans/001-web-foundation.md` as the execution checklist and updated durable decisions/current state accordingly.
- Next step: Codex executes the plan beginning with repo inspection and Nuxt bootstrap, not product/domain feature implementation.

## Resource gathering — building floorplans

- User wants to gather resources before starting implementation.
- Two local building images are available at `/home/farismnrr/Downloads/lt 1-1422.jpeg` and `/home/farismnrr/Downloads/lt 2-1422.jpeg`.
- Added `plans/000-resource-gathering.md` as the pre-implementation checklist.
- Codex should copy the images into `resources/buildings/building-1422/source/`, normalize them to `floor-1.jpeg` and `floor-2.jpeg`, and preserve the originals in `~/Downloads`.
- `1422` is only a local asset slug until Situm metadata confirms the actual building identifier.
- Situm building/floor identifiers should be discovered through the existing Situm API access where possible rather than requested manually from the user.
- PostgreSQL credentials and the Situm API key remain local-only and must never be written into repository documentation or memory.
- Current phase changed to resource gathering; after the ready checklist passes, execution continues with `plans/001-web-foundation.md`.

## Git workflow governance

- User established a mandatory plan execution workflow: each plan gets its own Git branch and worktree.
- Every completed implementation phase must update plan progress and `.agents/` persistence before commit, then commit and push the plan branch.
- Pull requests must not be created until the user explicitly asks for one.
- CI and unit tests are deferred to keep the early project simple.
- Nuxt lint is mandatory for code-changing phases once the Nuxt tooling exists, using the maintained Nuxt ESLint flat-config approach.
- Added `.agents/protocols/git-workflow.md` and routed it through root `AGENTS.md` so Codex automatically reads it for plan/repository implementation work.
- Added `plans/README.md` to make the same workflow visible from the plan directory.
- Plan 000 and plan 001 must use separate branches/worktrees; completing plan 000 does not authorize continuing plan 001 in the same branch.

## Resource gathering execution

- Worker copied and validated both floorplan JPEGs without recompression, and discovered Situm building `19866` with floors `69904`/`69905`.
- Added non-secret resource manifests and reusable Situm metadata.
- PostgreSQL inspection could not run because local `DATABASE_URL` was unavailable; no database changes were made.
- Plan 000 is blocked pending local database connection configuration.

## PostgreSQL resource follow-up

- Located the shared PostgreSQL Compose definition at `/home/farismnrr/Documents/shared/docker-compose.shared-infra.yml`.
- Confirmed healthy container `sensio-postgres` on `localhost:5432` and completed a read-only inventory of databases/schemas.
- Confirmed `situm_explore` is not currently present, so it remains the dedicated schema candidate; no database changes were made.
- Plan 000 resource gathering is now complete; next action is plan 001 in a separate worktree.

## Web foundation implementation

- Implemented Nuxt UI, `nuxt-auth-utils` session flow with verified owner credentials, protected dashboard, official Situm Map Viewer boundary, Drizzle/PostgreSQL wiring, runtime config, and setup documentation.
- Reviewed and applied the dedicated `situm_explore` migration only; no unrelated database objects were changed.
- Lint, typecheck, build, and local smoke tests passed. No credentials were persisted.

## Main worktree cleanup

- Synced local `main` to the merged web-foundation commit.
- Removed merged plan worktrees and local/remote plan branches for plans 000 and 001.
- Preserved pre-sync local changes in a stash rather than deleting them.
