# Sessions — 2026-08-12

This is a concise chronological session trace. Current durable truth lives in `.agents/state.md`, `.agents/memory/decisions.md`, `ARCHITECTURE.md`, and `DESIGN.md`; this file preserves history and may contain superseded decisions.

## Agent/context foundation

- Initialized the repository-native Codex context system with root `AGENTS.md` routing into `.agents/`.
- Established identity, state, memory, knowledge, reflections, session history, chat lifecycle, persistence, and Git workflow protocols.
- User wants durable context to improve future Codex work without turning memory into transcript hoarding.

## Initial web foundation

- Chose one full-stack Nuxt app using Nuxt UI, Nitro routes, `nuxt-auth-utils`, PostgreSQL/Drizzle, and Situm web SDK.
- Native/mobile, CI, and unit-test infrastructure were intentionally deferred.
- Implemented configured-owner real login/session, protected dashboard, `/api/me`, fixed `situm_explore` PostgreSQL schema, and real Situm Map Viewer integration.
- Applied/verified the application migration only and avoided unrelated database objects.
- Local credentials remained ignored/unpersisted.

## Foundation hardening

- User simplified plan execution to one dedicated plan branch in the normal working directory; linked worktrees are not used unless explicitly requested.
- Hardened public-resource policy, reproducible Nuxt ESLint setup, truthful Situm `MAP_IS_READY` lifecycle, fixed PostgreSQL schema configuration, and plan history state.
- User confirmed the remaining manual auth/API/Situm checks after hardening.
- Plan 002 was integrated via PR #3 after workflow correction.

## Plan 003 UI attempt and closeout

- Created and implemented a first minimalist SaaS UI refresh, preserving auth/database/Situm behavior.
- Plan 003 was eventually merged through PR #5 and closed.
- User later clarified it was closed because the result was too far from expectation, **not** because the rendered UI was accepted.
- Plan 003 is therefore historical implementation evidence only and must not drive future visual decisions.

## Approved interactive UI reference strategy

- User asked for an interactive HTML/CSS/JS prototype in chat to act as the visual/interaction reference for AI implementation.
- Prototype direction included landing, login/register, authenticated app, dashboard, map workspace, and Situm-related product surfaces with dummy interactions.
- Navigation-arrow product mark replaced the earlier `S` lettermark.
- User clarified the HTML/CSS/JS is **reference only**: production must use Nuxt/Vue/Nuxt UI and must not copy the prototype stylesheet/JS architecture.
- Root `DESIGN.md` became the single design router; duplicate `.agents/design/` and duplicate HTML/implementation-guide files were removed.
- Exactly one canonical HTML path remains: `design/reference/situm-explore-interactive-prototype.html`.
- The canonical file was reset to `Hello World` so the user can manually paste the approved prototype. While it is placeholder-only, agents must not infer/reconstruct visual UI from memory or generic references.

## UI and backend roadmap split

- Plans 004–009 were created as the UI roadmap.
- Existing real integrations remain real, while missing product domains use typed local dummy data to avoid backend scope explosion.
- Later plans 010–015 were created for post-UI Situm integrations.
- Every UI and later integration plan references the canonical HTML first so accepted composition is preserved while implementation/data sources change.

## Nuxt UI translation guard

- Tightened `design/IMPLEMENTATION.md`: Nuxt UI primitive -> props/variants/slots/tokens/utilities -> small composition -> small product component -> narrow custom CSS only when necessary.
- Explicitly prohibited copying prototype `.btn`/`.card`/`.pill` systems, raw screen-switching JavaScript, or wholesale CSS into production.

## Nuxt 4 architecture contract

- User requested SOLID, DRY, KISS, layered architecture, and clear folder mapping before the page count grows.
- Added root `ARCHITECTURE.md` as the single architecture/folder/dependency contract.
- Target runtime split: Vue application under `app/`, Nitro/server under root `server/`, genuinely cross-runtime contracts/pure utilities under root `shared/` only when needed.
- Architecture stays deliberately thin: pages compose routes, composables own reusable reactive coordination, API handlers own transport, server services/repositories appear only when real complexity justifies them.
- Plan 004 Phase 0 owns the one-time migration of current root Vue directories into Nuxt 4 `app/` and moves `server/utils/db.ts` to `server/db/client.ts` without changing runtime behavior.

## Time-boxed Situm POC setup

- User prioritizes PoC delivery speed.
- Situm environment naming was simplified to one `NUXT_PUBLIC_SITUM_API_KEY` plus `NUXT_PUBLIC_SITUM_BUILDING_ID`.
- User may provision the single POC key with Read & Write permission for speed and revoke/replace it after the POC.
- Plans 004–009 remain UI-first/dummy-first despite broader key permission.
- If local building ID is blank, agent may discover accessible buildings with `GET https://api.situm.com/api/v1/buildings` using `X-API-KEY`, then write only the selected ID to ignored local `.env`.
- API-key values are never persisted/logged in repository context.

## Final pre-execution roadmap audit

User requested a full repository/docs/plans/code audit before executing plans sequentially. The audit found and corrected several real gaps:

### Sequential branch dependency gap

- Previous workflow allowed new branches from `origin/main` while PRs remained user-gated, which could cause Plan N+1 to start without Plan N's unmerged implementation.
- Root `AGENTS.md`, `plans/README.md`, and `.agents/protocols/git-workflow.md` now require a dependent plan to wait until its prerequisite is complete and integrated into `main`.
- Stacked branches are allowed only with explicit user instruction.

### Plan 004 -> Plan 005 route gap

- Plan 004 previously risked redirecting login to `/app` before Plan 005 created the route.
- Plan 004 now ends independently usable: real login continues to existing `/dashboard` and the real Viewer remains reachable.
- Plan 005 atomically creates `/app/**`, changes login continuation to `/app`, moves unauthenticated app navigation to `/login`, and retires/redirects legacy dashboard UI only after the new routes exist.
- `/app/map` temporarily mounts the existing real Viewer in Plan 005 so the working integration is never lost between plans.

### UI dummy-first ambiguity

- Plan 006 previously allowed opportunistic SDK wiring for POIs/routes/camera/etc.
- Plans 006–009 and `design/IMPLEMENTATION.md` now explicitly forbid new Situm product-domain REST/SDK feature wiring during the UI roadmap.
- The only real Situm behavior during UI plans is the pre-existing Viewer create/readiness/error lifecycle; new surrounding controls remain local/dummy.

### Dummy fixture ownership gap

- Plans 005–007 now require canonical typed synthetic records under `app/data/prototype/` and reuse them across global search, Map, Cartography, and Operations instead of duplicating logical buildings/POIs/users.

### Plan 010 overlap gap

- Original Plan 010 duplicated actual domain integration work later repeated by Plans 011–015.
- Plan 010 is now feasibility/capability/data-contract mapping only and must not replace fixtures.
- Plans 011–015 are the sole sequential domain integration owners: Buildings/POIs -> Geofences/Paths/Routing -> Realtime -> Reports -> Organization/Users/Alarms.
- If real write actions are still needed afterward, they require a new explicit mutation plan rather than leaking into UI/read plans.

### Stale credential/docs/context cleanup

- Removed active `Only Read` assumptions from the current roadmap; current POC wording is Read & Write-capable while individual plans remain scoped to their intended behavior.
- Refreshed durable knowledge/preferences/goals/decisions/state so they no longer describe the project as pre-product or reference the old Situm env name.
- Corrected `package.json` product description.
- Clarified historical plans are evidence only and do not override current contracts.

## Current next step

- User manually replaces `design/reference/situm-explore-interactive-prototype.html` with the approved prototype.
- Plan 004 may begin with Phase 0 architecture/setup before that replacement, but visual Phase 1+ must stop if the HTML is still `Hello World`.
- Execute each dependent plan only after the prior plan has been reviewed, explicitly authorized for integration, and landed in `main`.

## Session — repository sync

- User requested pulling the latest code from `origin`.
- Verified clean `main` worktree, fetched `origin`, and fast-forwarded `main` from `cea80f1` to `6e99139`.
- Working tree remains clean; no durable context changes beyond this session trace.

## Session — Plan 004 Phase 0 architecture alignment

- User requested only Nuxt 4 architecture alignment.
- Migrated Vue application files into `app/`, moved DB initialization to `server/db/client.ts`, and preserved API URLs and runtime behavior.
- Validated with diff check, lint, typecheck, and build; visual phases remain gated on the populated canonical HTML reference.
