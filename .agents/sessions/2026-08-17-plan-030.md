# Plan 030 — Native Map, Positioning & Navigation

Date: 2026-08-17
Branch: `plan/030-native-map-positioning-navigation`

## Implementation evidence

- Rechecked the installed `@situm/react-native` 3.19.2 source and current official React Native guidance. The implementation uses the proven `MapView`, `SitumProvider`, dedicated API-key auth, User Helper, Remote Configuration, foreground `requestLocationUpdates`, floor/POI controls, and MapView navigation lifecycle.
- Added workspace-bound native Map loading from the authenticated workspace, explicit real-building selection, real floor/POI interaction, current-location-gated directions, location/status/error states, and teardown on workspace/building changes, app background, navigation away, and logout.
- No Read & Write or browser Viewer credential is returned to mobile. No background location, Realtime, Share Live Location, fake route metrics, fake positioning, or demo/default building fallback was added.
- The published package still omits its referenced `lib/` TypeScript files. A narrow local declaration boundary mirrors the exercised installed API surface; Android/Metro native integration remains through the package itself. This is not iOS runtime evidence.

## Validation

- Root: `git diff --check`, `npm run lint`, `npm run typecheck`, `npm run build`, `npm test` — pass; 13 tests pass.
- Mobile: `npm run lint`, `npm run typecheck` — pass.
- `npx expo-doctor` — 20/21; only the known `@situm/react-native` New Architecture metadata warning remains.
- `npx expo prebuild --clean --no-install` — pass.
- Android: `ANDROID_HOME=/home/farismnrr/Android/Sdk ANDROID_SDK_ROOT=/home/farismnrr/Android/Sdk ./gradlew assembleDebug -PreactNativeArchitectures=arm64-v8a --max-workers=2 --console=plain` — `BUILD SUCCESSFUL`; APK generated under ignored build output.
- Secret scan and full branch diff review performed; no credential values were added or persisted.

## Acceptance gate

Plan 030 is blocked at Phase 7. `adb devices` has no connected supported physical device; `mobile/.env` is absent; and no owner-authorized Positioning credential, calibrated building/profile, and reachable API runtime path was available for truthful acceptance. Therefore no real blue-dot, floor transition, POI, navigation, or device lifecycle evidence is claimed. Do not start Plan 031.
## Remediation closeout

- Fixed all six Plan 030 final-review blockers: credentialed `SitumProvider` ownership, workspace-keyed teardown, truthful fresh/stale/error position gating, navigation cancellation, cartography-backed POI resolution, and retry refetch.
- Added pure regression coverage for status transitions, current-location ownership/freshness, and POI lookup in `test/mobile-plan-030.test.ts` / `mobile/src/map/state.ts`.
- Root/mobile tests, lint, typecheck, Nuxt build, Expo prebuild, secret scan, and Android `assembleDebug` passed; build included x86/x86_64. Expo Doctor is 19/21 for the known Situm metadata warning plus frozen Expo patch-version notices.
- Emulator install/launch was not claimed: `adb` had no device at the final check. No safe backend/account/workspace credentials were available; physical positioning, blue-dot, floor transition, and real navigation remain blocked.
- Next action: commit and push this remediation; stop before PR/merge and do not start Plan 031.

## Second remediation closeout

- Resolved the two remaining blockers from the final review: truthful out-of-route copy and conditional navigation ownership/cancellation when positioning stops or lifecycle/building/workspace teardown occurs.
- Added regression coverage for idle/active/native-running navigation ownership; root tests pass 17/17. Root/mobile lint, typecheck, build, Expo prebuild, and Android `assembleDebug` pass. Expo Doctor remains 19/21 for the known metadata warning and frozen patch drift.
- Plan 030 remains blocked at mandatory physical-device acceptance: no connected device, mobile runtime env, owner-authorized credential, calibrated building/profile, or reachable acceptance evidence. Commit and push this branch only; no PR/merge and no Plan 031.
