# 2026-08-14

## Final local configuration readiness pass

- Synced clean `main` with `origin/main`; Plan 021 implementation was not started.
- Updated ignored local `.env` only: generated `NUXT_WORKSPACE_CREDENTIAL_ENCRYPTION_KEY` from 32 random bytes encoded as base64url, and configured standard host OTLP variables `OTEL_SERVICE_NAME`, `OTEL_EXPORTER_OTLP_ENDPOINT=http://127.0.0.1:4318`, and `OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf`.
- Validated the workspace key decodes to exactly 32 bytes and passes an AES-256-GCM local roundtrip. Session password remains configured at 32 characters.
- Using the exact final `.env`, PostgreSQL host authentication/`SELECT 1`/`situm_explore` schema access/current table visibility passed; ClickHouse authentication/`SELECT 1`/`situm_explore_analytics` database and analytics table visibility passed.
- Existing Docker infrastructure remains healthy: shared PostgreSQL and ClickHouse mappings are unchanged; Sensio Alloy remains healthy with loopback-only `127.0.0.1:4318:4318`; Grafana, Tempo, and Loki health/readiness passed. OTLP HTTP endpoint from final env accepted a host request; no true test-span ingestion was attempted because application instrumentation is Plan 023 scope.
- Production build, lint, typecheck, Compose validation, and preview HTTP smoke passed. Public routes booted, protected routes returned 401 unauthenticated, invalid login returned generic 401, and the configured private Situm REST buildings read returned HTTP 200 without exposing credentials. Public build assets contain neither the private Situm key nor workspace encryption key; the private Situm key is server-runtime-only.
- Google OAuth credentials were not found in searched local development configuration and remain external-deferred by design. Authenticated browser smoke was not repeated from this shell because only the existing password hash is local; no password was guessed or exposed.

## OpenTelemetry -> Alloy -> Tempo end-to-end smoke

- Verified the existing healthy stack from `/home/farismnrr/Projects/Sensio/docker-compose.obs.yml`: Alloy OTLP HTTP receiver `0.0.0.0:4318`, Alloy export to Docker service `tempo:4317`, healthy Tempo storage/query API, and Grafana Tempo datasource provisioning.
- A disposable isolated Node OpenTelemetry client loaded the final Situm Explore `.env` endpoint/protocol and emitted service `situm-explore-preflight` with root `preflight.otel.tempo` and child `preflight.downstream.test`. Exact Tempo lookup proved trace `92af0d2cc70260ec1d332291a727a0fa` contained both spans.
- Alloy showed no relevant receiver/export errors in the narrow post-test log window. Grafana health remained healthy; its Tempo datasource was confirmed by provisioning, while unauthenticated Grafana API access was not used for mutation.
- Controlled disposable bad-endpoint test to `127.0.0.1:1` failed cleanly with `ECONNREFUSED` and no hang. Recovery through the final endpoint emitted trace `e82429d7f3044e5df31a8fd00cc6fe19`, and exact Tempo lookup again proved both expected spans.
- Temporary smoke tooling and its isolated dependencies were deleted. No application source, package manifest/lockfile, `.env`, Docker/Compose configuration, or product behavior changed. OTLP -> Tempo infrastructure is fully proven; application instrumentation remains Plan 023 work.
## Stacked roadmap execution — Plan 021 Phases 1–2

- User authorized stacked Plans 021–025 execution without PRs or merges. Created `plan/021-auth-identity-foundation` from main HEAD `1996399`.
- Worker completed Plan 021 Phase 1 audit with no code changes.
- Worker added PostgreSQL/Drizzle `users` and `provider_identities` schema plus migrations. Applying with the exact local `.env` succeeded; safe metadata confirmed tables, normalized-email check, provider uniqueness, and ownership foreign key. Lint, typecheck, build, and diff checks passed.
- No secrets were persisted. Current branch remains `plan/021-auth-identity-foundation`; next phase is registration/password login.

- Worker implemented DB-backed registration and password login with normalized email, `nuxt-auth-utils` password hashing, generic invalid credentials, and safe duplicate handling. Bounded production-preview API smoke passed for registration, duplicate registration, invalid login, and unauthenticated protection; no secrets were exposed.
- Worker migrated sealed sessions to stable DB user IDs, removed env auth from runtime config, added logout, and made `/api/me` revalidate the DB user. Production-preview session/protection checks and baseline validation passed.
- Worker added conditional Google OAuth configuration and callback preparation with verified-email/provider-identity safeguards; absent credentials disable the route without startup failure. Build and baseline checks passed.
- Worker added the real registration page and auth links. Production-preview closeout initially exposed unavailable Lucide icon SSR fetches; the worker fixed this with the local `@iconify-json/lucide` dataset. Final lint, typecheck, build, and bounded auth/API preview smoke passed; Google runtime remains deferred.

## Stacked roadmap execution — Plan 021 closeout and Plan 022 Phase 1

- Plan 021 completed on `plan/021-auth-identity-foundation` and was pushed at `1494a1221f09acb0c0d1c98e9ee5d4633ae41465`.
- Created `plan/022-workspaces-situm-credentials` directly from that exact HEAD.
- Worker added and applied the private single-owner `workspaces` schema/migration with owner foreign key; no Situm consumers or credentials changed. Diff, lint, typecheck, build, and migration metadata checks passed.
- Worker added authenticated workspace create/list/get/update/delete APIs with owner-scoped queries and cross-user denial. Bounded two-user workspace smoke and baseline validation passed; no credentials are returned or logged.
- Worker added versioned AES-256-GCM workspace Situm credential envelopes, server-only decrypt support, fail-closed key validation, and owner-scoped safe config GET/PUT endpoints with VIEW_ONLY/VIEW_WRITE metadata. Migration and encrypted persistence/replacement/metadata smoke passed; raw credentials were not returned.
- Worker added owner-scoped non-destructive Situm configuration validation using the installed SDK organization read, with safe validation/mismatch errors. Plan 022 closeout passed workspace ownership, encrypted config, permission metadata, retained baseline, lint, typecheck, build, and production-preview checks; no consumer migration was started.

## Stacked roadmap execution — Plan 022 closeout and Plan 023 Phases 1–2

- Plan 022 completed on `plan/022-workspaces-situm-credentials` and was pushed at `654c56d242aea282d19e5dd6af3193c4e16afe48`.
- Created `plan/023-observability-error-boundary` directly from that exact HEAD.
- Worker added the application OTLP exporter/plugin using standard environment configuration, request middleware with W3C traceparent and safe x-request-id propagation, plus nested DB and Situm operation spans. `/api/me` production smoke passed with propagated headers; the Situm cartography smoke was not used because the upstream call hung. No secrets were logged.

- Plan 023 Phase 3 completed on `plan/023-observability-error-boundary`: worker added a Nitro error hook that normalizes expected failures to safe codes/messages, preserves a request ID, and annotates active telemetry without exposing internal details. Invalid workspace IDs now fail safely before database access. Lint, typecheck, production build, and diff checks passed. No secrets were logged.
- Plan 023 Phase 4 completed on `plan/023-observability-error-boundary`: production preview emitted an application trace for an authenticated `/api/me` request. Exact Tempo lookup succeeded for trace `c8f5d2e34b397cf2d415f4affb5288c8`, with `http.request`, `db.user_lookup`, and `db.foundation_lookup` spans. A controlled sanitized failure path and recovery smoke passed without secret exposure. Final lint, typecheck, build, and diff checks passed.
- Created `plan/024-workspace-situm-backend-migration` directly from Plan 023 HEAD `a2e11c8b6d5864a28a97811cd00d27341965a295`. Phase 1 added reusable owner-scoped workspace Situm credential/client resolution and explicit workspace routes for config, organization, users, status, and cartography. Route parameter mismatch was corrected from `id` to `workspaceId`; lint, typecheck, and build passed. The authenticated upstream status smoke hung and was terminated without changing global fallback or exposing credentials.
- Plan 024 Phase 2 added explicit workspace-scoped paths and realtime read routes using the same owner-checked server-side credential context. Lint, typecheck, and build passed; bounded preview boot/config succeeded, while upstream-backed routes returned sanitized 404 responses and no credentials were exposed.
- Plan 024 Phase 3 added workspace-dimensioned ClickHouse tables/queries and owner-checked analytics summary/sync routes. Workspace sync now decrypts the selected credential per request, writes only workspace-scoped rows, and leaves legacy unscoped rows untouched/excluded. Unauthorized/missing-config smoke, lint, typecheck, and build passed; no secrets were exposed.
- Plan 024 closeout removed the migrated `[id]` Situm config route authority in favor of explicit `[workspaceId]` routes, including validation. Final worker review passed diff-check, lint, typecheck, build, and bounded preview checks. No secrets were exposed; Google OAuth and external Situm permission variants remain manual acceptance items.
- Created `plan/025-workspace-ux-regression` directly from Plan 024 HEAD `4870a4465e892fc6512111261e26f23ac07a4e50`. Phase 1 added the workspace context composable and authenticated workspace management/configuration page, keeping raw Situm keys write-only. Runtime route matching was diagnosed and fixed with a bounded workspace-path handler; lint, typecheck, build, and create/session/config preview smoke passed. No secrets were exposed.

## Plan 025 Phase 2 closeout

- Production preview regression passed with disposable users: registration, `/api/me`, protected/unauthenticated behavior, workspace create/rename/delete, cross-user denial, config GET-before-write 404, PUT 200, GET safe metadata 200, validation 422 for an invalid disposable key, and no raw key in the response.
- Authenticated `/app/workspaces` and `/app/map` rendered in production preview; logout followed by session lookup returned 401 when the updated cookie jar was used. Analytics returned a bounded validation response and upstream cartography failure was sanitized; no client secret was exposed.
- `git diff --check`, lint, typecheck, and build passed. Preview processes were terminated. Google OAuth and external Situm permission/upstream acceptance remain user-owned/manual.

## Plan 025 security remediation and analytics readiness

- Confirmed legacy global Situm and analytics paths were still present; added a safe 410 middleware fence and moved supported page consumers to owner-scoped workspace endpoints. Unsupported groups, alarms, and geofences remain unavailable rather than using global fallback.
- Disabled the browser Viewer because installed SDK evidence did not prove a scoped least-privilege browser credential exchange; it now surfaces truthful guidance and never reads public/global Situm credentials.
- Workspace analytics summary/sync now idempotently ensure the configured ClickHouse database contains workspace tables before access. Production preview returned a safe empty workspace summary and metadata verified all four workspace tables; legacy routes returned 410 and no keys were exposed.

- Added explicit owner-scoped workspace alarms list/detail routes using the per-request decrypted workspace key; invalid disposable credentials returned a sanitized upstream 502. Removed Situm credentials and building identifiers from Nuxt public/private runtime config and `.env.example`; health reports workspace authority only. Fresh production preview confirmed config write, alarms route, legacy 410 fencing, health, alarms page, and map page behavior. Lint, typecheck, build, and diff-check passed.

## Targeted Situm authentication / Viewer security smoke

- Tested two temporary user-supplied Situm API keys server-side only; both authenticated successfully and passed harmless organization/building reads. No credential values or JWTs were persisted or reported.
- With installed `@situm/sdk-js` v0.25.0, the access-token flow is `/api/v1/auth/access_tokens`; bearer JWT authentication also passed harmless reads. Sanitized JWT metadata showed approximately 24-hour lifetime, `api_permission=read-only` for the read-only key, and `api_permission=read-write` for the read-write key.
- The SDK's `Viewer.setAuth(jwt)` accepts a JWT via postMessage, but the read-write-derived JWT retains broad write authority. This is Outcome B: it is not safe as a least-privilege browser Viewer token.
- A disposable standalone Viewer probe sent `setAuth` without exposing the long-lived API key; headless cartography readiness was not conclusively observed. The application Viewer remains intentionally disabled. No code/config changes were made.
- Temporary scripts, tokens, screenshots, copied SDK files, and local server were removed. Tracked-file scan found zero temporary-key matches; working tree remained clean at the existing Plan 025 HEAD.
- Follow-up decision: keep the Viewer security blocker. A separate read-only/scoped Viewer credential may be considered only after explicit Situm evidence and a successful actual Viewer/cartography smoke. The user later superseded immediate revocation and authorized bounded reuse until final acceptance passes.

## Plan 025 continuation attempt

- The user superseded the earlier immediate-revocation reminder and authorized continued bounded acceptance with the same temporary read-only/read-write keys until Plan 025 passes. This policy is reflected in current state/preferences/decisions.
- The keys were not present in the local `.env` or current local session, and were not recovered from Git history or persisted anywhere. No implementation or configuration changes were made.
- Result: blocked before Viewer retest because the required temporary credentials are unavailable locally. Do not revoke them yet; final reminder remains deferred until acceptance can be completed.

## Plan 025 final Viewer/security acceptance

- Loaded the temporary read-only and read-write Situm keys from the ignored local `.env`; values were never printed or persisted.
- Server validation passed for both keys: read-only/read-write permission metadata matched, and harmless bearer reads passed.
- Production-preview real-app acceptance configured a disposable workspace with the read-write primary credential and read-only Viewer credential. Safe config metadata returned no raw keys; the owner-scoped Viewer route returned a verified read-only JWT; cross-user Viewer/config access was denied.
- Chrome production-preview acceptance reached the app Viewer `Ready` state. The actual cross-origin Situm iframe rendered a `783x495` canvas with cartography content and no error text. Navigate-away/back acceptance restored the authenticated app route. Browser storage contained only workspace/theme state; long-lived keys and the read-write JWT were not exposed.
- Exact Tempo lookup for an authenticated Viewer-token request found `http.request` plus `workspace.viewer_auth` spans. Span attribute keys were limited to request/workspace metadata; no credential/JWT material was present. Tracked files and public build assets contained zero loaded-secret matches.
- Fixed the SDK readiness race and added the safe Viewer-auth span in commits `7bedeb3` (on top of `4dcd1b9`). Lint, typecheck, build, diff-check, production preview, auth/workspace/security smoke all passed.
- Plan 025 final acceptance is PASS with Google OAuth runtime remaining external/manual. Temporary keys remain in ignored `.env` until the user revokes/deletes them after this PASS.

## Plan 025 handoff continuation

- Read the current identity/state/protocol/memory/architecture/plan/design authority before continuing Claude's work.
- Validated the existing uncommitted workspace-context and UI cleanup patch with `git diff --check`, lint, typecheck, and production build; all passed.
- Bounded preview smoke passed for `/`, `/login`, protected `/app/map`, `/api/me`, and the removed `/app/settings` route (404 as intended). No secrets were exposed.

## Initial-loading UX regression remediation

- Delegated the loading-state audit to the worker after the user reported fixture/mock content and mismatched text appearing during initial loads.
- Removed the home-page prototype building/floor SVG fallback and prototype home data import; the real workspace preview now renders only after a successful cartography response, with loading/error/empty states.
- Made workspace-backed pages truthful during `idle`/`pending`: dashboard, buildings, POIs, paths, groups, alarms, geofences, users, organization, realtime, analytics, and workspace status no longer show fake counts or empty results before their requests succeed.
- Replaced premature health/count copy with loading/unavailable states. Parent validation passed diff-check, lint, typecheck, and production build.

## Nuxt skeleton loading refinement

- Replaced verbose data-loading alerts with Nuxt UI `USkeleton` placeholders across workspace-backed data regions, including the Home building preview.
- Kept page-level error and post-success empty states, while removing the remaining Home welcome loading sentence so loading is represented visually by the skeleton.
- Parent validation passed diff-check, lint, typecheck, and production build.

## Dashboard metrics feedback

- Delegated the `/app/dashboard` metric-panel fix to the worker.
- Replaced the unresolved metrics explanation with workspace-scoped analytics for the last 7 days: reported visitors, positioning time, and geofence stay time. Each metric retains its source semantics; no incompatible Situm resource totals or mock values were introduced.
- Added skeleton/loading, safe error, and no-synced-data states. Worker and parent validation passed.

## Map layout feedback

- Delegated `/app/map` layout remediation: removed the inner viewer padding, added full-width/min-width sizing, and kept the existing desktop-only gate so mobile/tablet does not mount the Situm Viewer and instead receives desktop guidance.
- Worker validation passed; the parent reviewed the diff and confirmed the Viewer remains the single lifecycle owner.

## Map full-height screenshot follow-up

- A desktop screenshot showed the Viewer stopping at its old ~22rem minimum height with a large blank area below. Delegated a second sizing fix.
- Established an explicit desktop map workspace height and propagated `height: 100%`/`min-height: 0` through the map slot, `SitumViewer` card/body/container, and SDK-created iframe. Mobile gating remains unchanged.
