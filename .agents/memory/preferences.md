# Preferences

## Workspace preferences

- Wants the Codex-facing root `AGENTS.md` to point into a dedicated `.agents/` context directory. `(source: user-stated)`
- Wants conversations to update persistent context such as memory and knowledge rather than remain isolated chats. `(source: user-stated)`
- Wants the current phase limited to the agent foundation, without starting product/project implementation yet. `(source: user-stated)`
