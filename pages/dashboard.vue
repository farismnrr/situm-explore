<script setup lang="ts">definePageMeta({ middleware: 'auth' }); const { user, clear } = useUserSession(); const { data, error } = await useFetch('/api/me'); async function logout() { await clear(); await navigateTo('/') }</script>
<template><main class="mx-auto max-w-3xl space-y-6 p-8"><div class="flex justify-between"><h1 class="text-2xl font-semibold">Dashboard</h1><UButton variant="outline" @click="logout">Log out</UButton></div><UCard><p>Signed in as {{ user?.email }}.</p><p class="mt-2">Database: {{ data?.status || error?.statusMessage || 'Loading…' }}</p></UCard><SitumViewer /></main></template>
