<script setup lang="ts">definePageMeta({ middleware: 'auth' }); const { user } = useUserSession(); const { data, error } = await useFetch('/api/me')</script>
<template><AppShell><div class="mx-auto max-w-3xl space-y-6"><div><h1 class="text-2xl font-semibold">Dashboard</h1></div><UCard><p>Signed in as {{ user?.email }}.</p><p class="mt-2">Database: {{ data?.status || error?.statusMessage || 'Loading…' }}</p></UCard><SitumViewer /></div></AppShell></template>
