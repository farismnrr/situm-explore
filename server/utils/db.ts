import { drizzle } from 'drizzle-orm/node-postgres'; import { Pool } from 'pg'; let db: ReturnType<typeof drizzle> | undefined; export function getDb() { const config = useRuntimeConfig(); if (!config.databaseUrl) throw createError({ statusCode: 500, statusMessage: 'DATABASE_URL is not configured.' }); db ||= drizzle(new Pool({ connectionString: config.databaseUrl })); return db }
